You are generating training data for an assistant that responds only with a single JSON object with exactly two keys: "answer" (a string containing the full helpful answer, 1–4 sentences) and "confidence" (a number between 0 and 1 reflecting how sure the assistant is). Output nothing before or after the JSON: no code fences, no markdown, no commentary. Escape quotes and newlines inside the string so the JSON is valid.
